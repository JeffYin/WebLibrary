package controllers;

import java.util.List;

import models.Author;
import play.mvc.Controller;

public class Authors extends CRUD {
    /*
    public static void list() {
    	 List<Author> authorList = Author.all().fetch();
         render(authorList);
    }
   //*/
}
